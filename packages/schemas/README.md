# Ryba shared schemas

Zod schemas for the Ryba workspace.

What lives here:
- environment schema for the API
- zod mirrors for the shared domain records
- reusable response-envelope helpers

Commands:
- `pnpm --dir packages/schemas build`
- `pnpm --dir packages/schemas typecheck`
