# Ryba shared types

Shared TypeScript domain types for the Ryba workspace.

What lives here:
- entities
- relations
- canvas layout model
- document entity references
- API envelope primitives

Commands:
- `pnpm --dir packages/types build`
- `pnpm --dir packages/types typecheck`
